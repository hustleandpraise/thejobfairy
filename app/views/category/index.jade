extends ../layout

block content

    h1= "Category: " + title

    each tweet in tweets
        p
            a(href="/user/" + tweet.user.username)= "@" + tweet.user.username + " : "
            = tweet.text
